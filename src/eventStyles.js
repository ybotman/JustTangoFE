const eventStyles = (category) => {
    let backgroundColor;
    let textColor;
    let fontStyle;
    let fontWeight;
    let fontSize;
    let borderWidth;
    let borderStyle;
    let borderColor;

    // Your switch statement and style definitions

    return {
        backgroundColor,
        textColor,
        fontStyle,
        fontWeight,
        fontSize,
        borderWidth,
        borderStyle,
        borderColor,
    };
};

export default eventStyles;
